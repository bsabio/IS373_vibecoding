<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% if title %}{{ title }} — {% endif %}AART</title>
    <meta name="description" content="{{ description | default('AART — Bauhaus-inspired experiments') }}" />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    {% include "partials/header.html" %}

    <main class="container">
      {{ content | safe }}
    </main>

    <footer class="site-footer">
      <div class="container">© AART — Bauhaus experiments</div>
    </footer>

    <script src="/include-partials.js"></script>
  </body>
</html>
